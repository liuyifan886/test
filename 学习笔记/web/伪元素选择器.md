作用：创建虚拟元素(伪元素)，用来摆放装饰性的内容，可以是数字，也可以是图片其他的
选择器：
- E::before   在E元素里面最前面添加一个伪元素
- E::after  在E元素里面最后面添加一个伪元素
其实写一个：也没有关系，但是在工作中一般都使用两个冒号
注意点：必须设置content："  "属性，用来设置伪元素的内容，如果没有内容，则引号留空即可，要是不写该选择器就不会生效
伪元素默认是行内显示模式
权重和标签选择器相同
